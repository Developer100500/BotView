<Window x:Class="BotView.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BotView"
        xmlns:chart="clr-namespace:BotView.Chart"
        mc:Ignorable="d"
        Title="MainWindow" Height="800" Width="800">
    <DockPanel>
        <!-- Header -->
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
            <Label Content="BotView" FontSize="18" FontWeight="Bold"/>
        </StackPanel>
        
        <!-- Status Bar at Bottom -->
        <StatusBar DockPanel.Dock="Bottom" Height="30"/>
        
        <!-- Main Content Area with Grid -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Left Menu Panel -->
            <StackPanel Grid.Column="0" Background="LightGray">
                <Label Content="Menu here" />
            </StackPanel>
            
            <!-- Chart View - Takes all remaining space -->
            <chart:ChartView Grid.Column="1" />
        </Grid>
    </DockPanel>


</Window>
